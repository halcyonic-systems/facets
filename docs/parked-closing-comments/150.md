Closing per the W30 ratification audit triage (#234). The substance is preserved in the repo's parked ledger:

**→ [`docs/parked.md` — #150 Rung 3, the extraction theorem](https://github.com/halcyonic-systems/bert-lenses/blob/main/docs/parked.md#i150)**

This issue records two things worth keeping and neither is a task.

**A decision:** rung 2 (embedded oracle) is **not adopted**, on a measurement rather than a preference. The recorded rule was "only if oracle latency bites"; measured 2026-07-21, the full 365-model corpus is one batched subprocess call — ~30 ms of oracle process time, 0.29 s total test wall time. Re-open only if call volume grows orders of magnitude, e.g. per-model property tests replacing the batched corpus. That is a live, named condition, and it belongs to rung 2.

**A scoped fidelity upgrade with two open decisions in front of it:** where the extracted Lean artifacts live (SSF vs a local Lake package — a real fork, since SSF keeps one toolchain but couples this repo's CI to an external pin, and a local package inverts both), and how hard to pin Charon/Aeneas/Lean, whose drift is the pipeline's documented failure mode. Either would have to be settled before an hour of work is worth spending.

**And the sentence that keeps this from reading as a gap:** rungs 1 and 1.5 already give two-sided falsifiability, and `spec/LENS_ENTRY_SPEC.md` §C presents the mapping as hand-mirrored rather than proved. **Nothing currently overclaims.** This work would remove the hand-mirroring; it does not fix a misstatement.
