Closing per the W30 ratification audit triage (#234). The substance is preserved in the repo's parked ledger:

**→ [`docs/parked.md` — #172 The neutral interchange](https://github.com/halcyonic-systems/bert-lenses/blob/main/docs/parked.md#i172)**

This issue was filed as a frame to design against — "blocked on nothing; blocks nothing" — and its durable artifact is the design doc, `docs/design/neutral-interchange.md`, which is unaffected by this close.

What the ledger preserves beyond the pointer is the **consolidation call**, which is the part that would have been lost: #67-thread-1 and #121 are special cases of this general interchange rather than separate pipelines, #112 owns the compose semantics with the Baez cospan mapping feeding it, #166 is the same "compose by shared variables" seam, and #67 lays the first brick forward-compatibly. Four issues became consumers of one interchange, deliberately.

Also kept: the core claim — the interchange is **not "CSV for a domain," it is a serialization of the kernel**, so domain legibility falls out of the lenses for free — and the explicit out-of-scope list, the "long conversation": exact schema, 3-vs-4 sheets, the tether refactor (subsume, do not extend), the provenance UI grammar, the LLM ingest(Observed)/generate(Generated) discipline, and how far to lean on decorated cospans versus a pragmatic first cut.

No unpark trigger, by the issue's own text.
