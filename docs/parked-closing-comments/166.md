Closing per the W30 ratification audit triage (#234). The substance is preserved in the repo's parked ledger:

**→ [`docs/parked.md` — #166 External↔external flows render as stubs](https://github.com/halcyonic-systems/bert-lenses/blob/main/docs/parked.md#i166)**

The finding is kept in full — the live DOM evidence distinguishing `photosynthesis` from the three first-class edges, the root cause (edge classification is boundary-relative, so a flow with both endpoints external has no boundary relationship to anchor to and `EdgeView` falls through to a non-interactive dashed stub), and the reason it matters: **the tuple permits environment↔environment relations and the model compiles clean; the lens just cannot draw it, and degrades it silently.**

Also kept: the reading that makes this a design question rather than a bug. It is **orthogonal to concrete-vs-conceptual**, and the general tuple does not forbid these relations, so the boundary-relative edge model is a **Mobus-lens simplification, not a formalism limit.** The three candidate semantics — (a) first-class edge type, (b) reject or warn at author time, (c) keep it degraded but explicit and never silent — are recorded as an open choice.

The canonical doctrine already lives in `docs/theory-fidelity.md` → *The single system-of-interest scope (Mobus lens) and the systems-of-systems seam*, plus its Honest-boundary-table row. This issue was the tracked tool gap; the ledger now holds it. What would decide the semantics is a real systems-of-systems modelling need, not an argument.
