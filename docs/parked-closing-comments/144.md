Closing per the W30 ratification audit triage (#234). This is the longest entry in the repo's parked ledger and nothing was compressed out of it:

**→ [`docs/parked.md` — #144 Formalizing the system life cycle](https://github.com/halcyonic-systems/bert-lenses/blob/main/docs/parked.md#i144)**

Closing this issue does **not** close the work — it moves the record out of a container that was mis-signalling it. The live work is the paper, and the paper's plan lives in SSF (`docs/paper/mobus-lifecycle-paper-plan.md`), not here. That plan's gate was "only results that are done or directly entailed"; §4 was the unmet one and SSF #30 / PR #34 met it on 2026-07-25, so **the next move is drafting §5, not more formalization.** The Increment 1/2 work this issue tracked is deliberately *after* George has the paper.

Preserved in full: the `F(S) = F_coh(S) ∩ F_φ(S)` thesis and why the derivable/stipulated boundary is the principled/arbitrary one; all five findings against the paper with the lines that license them; the architecture pass (`860efc7`) and its three corrections — the Origination result had built a tower, coherence is axis B rather than a structural conservation law, and "Bunge and Klir are static" is false and refuted by citations in this repo; the narrower real first increment (a typed structural diff, and the component-identity problem that makes it publishable either way); the git test; the hard line that none of it reaches `circuit.rs`; the paper's spine, scope and two-beat presentation to George; the four status corrections after the closure theorem landed — including that **the field set is the specification**, so any `F_coh` derived against five coherence fields is now under-constrained by SSF #31's sixth; the `replaceInterface`/`Step` limitation this issue predicted and the merged work has; and what is owed to George (his H insight, cited as his; his unintegrated adaptrode/EWMA mechanism) versus what is routed internally (the two `bert` doc defects).

If the formalization resumes, it resumes as a bert-lenses extension per the architecture pass, starting from the typed structural diff.
