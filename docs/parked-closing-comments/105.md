Closing per the W30 ratification audit triage (#234). The substance is preserved in the repo's parked ledger:

**→ [`docs/parked.md` — #105 Constellation authoring](https://github.com/halcyonic-systems/bert-lenses/blob/main/docs/parked.md#i105)**

Two reasons, and the first is the one that matters.

**The stated trigger has already fired.** This issue gates itself on "Nothing here starts until … the #89 dependency ladder" and "Once #89 ships…". **#89 closed 2026-07-21.** The gate is open; the issue was parked anyway, on a different and unstated basis — that the epic is large, unstarted, and not yet demanded by usage. That is a defensible reason. It is not the reason written down, and a trigger that fired and was not honoured is indistinguishable six months later from a trigger that was wrong. The ledger restates it honestly: **deferred on demand, not on dependency** — it waits until the constellation scale actually arrives in usage.

**The near-term slice shipped and the epic did not.** PR #119 (2026-07-21) delivered the reference-graph tree in the library and Switch menu. The four constellation-authoring surfaces are unbuilt. The ledger keeps both, along with the field note that sharpened the frame (SOI vs inventory), the promotion-rider caveat that SOI-ness is a reading rather than an intrinsic property, and the orphan-badge finding — that a deleted parent's ex-child is graph-indistinguishable from a hand-authored root, so the badge is unimplementable read-side and doctrinally should be.

Nothing here is foreclosed. If the demand arrives, open a fresh issue for the requirement and link back to the ledger entry for the reasoning.
