Closing per the W30 ratification audit triage (#234). The substance is preserved in the repo's parked ledger:

**→ [`docs/parked.md` — #126 Illustrated quickstart](https://github.com/halcyonic-systems/bert-lenses/blob/main/docs/parked.md#i126)**

The finding stands and is worth keeping in a form that does not expire: **bert-lenses is a visual instrument whose quickstart is text-only.** The product's core idea — a verdict panel refusing loudly and citing the formal precondition it rests on — is conveyed only in prose, and no paragraph communicates it as fast as one annotated image. The four-figure plan (author `bathtub.sl` → the canvas render → **a verdict panel citing a failed precondition, the money shot** → the fix and a passing verdict) is recorded verbatim.

**Half of the acceptance criteria is now met.** This issue's second bullet — "stale #80 references in README and quickstart are updated or removed" — is done in the same change that created the ledger; #80 closed 2026-07-20 without the walkthrough shipping, and the docs no longer defer to it. The figures themselves remain unbuilt.

Like #125 and #127, this was approved and then parked with no trigger; the ledger names that state rather than leaving it implicit. The obvious moment to build the figures is the next time the quickstart is walked end to end on a real machine — the screenshots are a by-product of that walk, not a separate project.
