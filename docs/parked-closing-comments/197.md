Closing per the W30 ratification audit triage (#234). The substance is preserved in the repo's parked ledger:

**→ [`docs/parked.md` — #197 Mobus's illustrative models as executable examples](https://github.com/halcyonic-systems/bert-lenses/blob/main/docs/parked.md#i197)**

This issue was filed as PARKED — "recorded, not scheduled" — which makes it the clearest case for the move: it was already a ledger entry wearing an issue's clothes.

The ledger keeps the rule it established, which outlives the work itself:

> **The corpus holds what the source says. Examples hold what we say.**

And the discipline that produced it: no `primitive` on any ch. 7 component, one `gate: core` on the brain because the source walks only the afferent path, an untyped disturbance flow on the HSS — each a place where stamping our reading would have been our reading, not his.

Also kept: why it is a program rather than a task (5 models × primitive assignment + dynamics + validation, each a genuine modelling judgment that wants Shingai's eye); the companion split with the K≅2 cross-check, which is different work with different outputs and deliberately not bundled; the shared standard from `assets/corpus/README.md` — **a model that cannot embarrass us is not testing anything**; and the sharpest point, which is about this half specifically: **the diffs between a corpus entry and its finished example are the findings.** If finishing a model produces no surprise, either the model was already complete or we finished it toward what the tool made convenient. Both are worth noticing.

The prerequisite for unparking stands and is a condition on doing it well rather than an event that fires: the primitive assignments must be defensible as *our* reading, stated as such, and never smuggled back into the corpus.
