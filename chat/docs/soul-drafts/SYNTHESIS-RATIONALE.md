# Synthesis rationale

What I took from where:

- Skeleton and spine: The Convergence Host (V2). Its "Stage, cut, converge, part" loop and the felt-convergence framing ("real minds disagreeing about everything and meeting at the structure," "four hostile traditions keep meeting") are the load-bearing architecture, because K≅2-as-felt-drama is the whole reason Facets exists. Kept V2's per-lens pet-peeves block and its faithful staging of the ontologist-vs-epistemologist tension.

- Warmth coat: The Delighted Polymath (V1). Grafted the named core move "Turn It In The Light," the recurring payoff "oh, look what was in there," the native opening "Here's the part that's easy to miss" (explicitly not "Great question"), the Bunge deep cut "no permanent strays or isolates," and the tender-and-merciless-in-one-sentence voice note. This keeps the convergence drama alive rather than clinical.

- Rigor spine: The Rigorous Guide (V3). Imported verbatim "Do not perform rigor. Rigor is the correct decomposition, not the vocabulary around it," "One correct decomposition beats four gestures at one," "systemhood is thinghood-independent" (cleanest K≅2 line), and "relations without relata are empty." Used V3's economy to cut mood-only lines and hold to one page.

What I cut / fixed:
- Neutralized V2's persona drift. Removed the MBTI-per-register assignments (anatomist ISTJ, etc.) entirely. Kept only the host's INTJ and converted V2's "distance between registers" into an explicit single tunable dial (register distance). Added a one-mind, "tonal shifts not costumes" assertion in Identity and Personality.
- Stripped every em-dash from the soul's own prose (all three source variants violated their own rule). Replaced with commas, colons, or sentence breaks throughout.
- Trimmed V1's pure-mood lines ("one mind that got greedy," "your standing joy is that the structure was there the whole time") that failed the every-line-behavioral test.
- Kept safety/format rules OUT: no JSON, classifier, or refusal policy here. The only boundary lines retained are voice-level (off-corpus refusal as a failure, don't-fake-convergence), which shape what the model says, not operational plumbing.

Open questions for Shingai:
1. Warmth level. I set it at "delighted and exact," warmer than V3, cooler than LemonAid. The register-distance dial is the intended tuning knob; you may want a second explicit warmth dial if the delight reads as too much for a "colder, sharper instrument."
2. Does the four-voices section earn its length? It is the longest block and the least behaviorally dense per line (some is descriptive characterization). If you need to reclaim space for the loader, this is where to compress: each voice could drop to a single sentence of real commitment plus its idiom, at some cost to fidelity vividness.
3. Loader seam. This is written as pure VOICE, so it should compile into the persona/system-preamble slot with the JSON-output contract, the corpus-classifier, and refusal policy layered on top from the prompts, not from here. Confirm the loader concatenates soul + operating-rules in that order, and that the eight-coordinate "under the hood" language will not collide with whatever structured-output schema the anatomist register has to emit.